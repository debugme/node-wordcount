import assert from 'assert'
// import Square from '../source/square'

// describe('square', () => {

//     let square = new Square(5)

//     it('should have correct area', done => {
//         assert.equal(square.getArea(), 25)
//         done()
//     })
// })

describe('true', () => {
  it('should equal true', done => {
    assert.equal(true, true)
    done()
  })
})


// Tests for Synchronous code
// [file.test.txt]
//
// (1) Test that words of    different case are  counted as the   same word
// (2) Test that words with a double-dash are concatenated
// (3) Confirm that non-letters are stripped out
// (4) Confirm that word counts are correctly identified as prime or nanotimer
// (5) Confirm that words are counted correctly